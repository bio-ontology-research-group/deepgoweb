# DeepGOWeb dependencies — coherent security upgrade 2026-06.
# The previous master pinned Django 5.0 alongside allauth 0.44 / DRF 3.9.2 and the
# memcached backend removed in Django 4.1, an inconsistent set that cannot run. This
# is a coherent, mutually-compatible, security-current stack on Django 4.2 LTS
# (supported to 2026). Targets Python 3.11: TensorFlow 2.15 supports 3.9–3.11 and is
# the last TF whose bundled tf.keras is Keras 2, so the legacy data/model.h5 loads
# unchanged (validated: model load + predict, GO pickles under pandas 2.x).
# (Django 5.2 LTS is a viable alternative but needs extra code changes — USE_L10N /
#  index_together / CachedStaticFilesStorage removals; deferred to keep this low-risk.)

# --- Django stack (4.2 LTS) ---
Django==4.2.30
djangorestframework==3.15.2
django-allauth==65.14.1  # clears open-redirect + token CVEs; login.html stripped of
                         # the removed get_providers/provider_list social tags (social
                         # login is not configured here)
django-widget-tweaks==1.5.0
django-cookie-law==2.2.0
defusedxml==0.7.1
psycopg2-binary==2.9.9
sqlparse==0.5.5

# --- async / cache ---
celery==5.4.0
redis==5.2.1
pymemcache==4.0.0        # Django 4.1 removed the python-memcached backend

# --- ML serving (TF 2.15 = last tf.keras on Keras 2; loads the legacy model.h5) ---
tensorflow-cpu==2.15.1
numpy==1.26.4
pandas==2.1.4
h5py==3.10.0

# --- HTTP / auth (CVE fixes) ---
requests==2.34.2
urllib3==2.7.0
certifi==2024.8.30
idna==3.18
charset-normalizer==3.4.0
oauthlib==3.2.2
requests-oauthlib==2.0.0
python3-openid==3.2.0
PyJWT==2.13.0
cryptography==49.0.0

# --- misc ---
Markdown==3.10.2
python-dateutil==2.9.0.post0
